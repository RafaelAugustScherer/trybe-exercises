//Utilize o for para imprimir os elementos da lista groceryList com o console.log() :

let groceryList = ['Arroz', 'Feijão', 'Alface', 'Melancia'];

for (idx = 0; idx < groceryList.length; idx += 1) {
    console.log(groceryList[idx])
}